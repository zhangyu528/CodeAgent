[install]
# Auto-install missing packages

[build]
# Alias @codeagent/core to the source directory.
# This is read by 'bun build' when bundling. CLI build script
# uses --target=node and --outfile flags instead.
[[build.alias]]
from = "@codeagent/core"
to = "./src/apps/core/index.ts"
