/**
 * CodeAgent directory paths — thin wrapper around SDK's getAgentDir().
 * All paths are derived from SDK default (~/.pi/agent/).
 */
import { getAgentDir } from '@mariozechner/pi-coding-agent';

export { getAgentDir };
